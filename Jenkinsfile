pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Building the application..."'
                sh 'sleep 3'
                sh 'echo "Build completed."'
            }
        }
    }
}